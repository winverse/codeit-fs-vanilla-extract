import { style } from '@vanilla-extract/css';
import { vars } from '@styles/theme.css.js';
import { tokens } from '@styles/tokens.css.js';

// TODO: Input 기본 스타일을 작성하세요
// 힌트: width, padding, borderRadius, border, backgroundColor,
//       fontSize, outline, transition, :focus, :disabled 스타일이 필요해요
export const inputBase = style({
  // 여기에 Input 스타일을 작성하세요
});
